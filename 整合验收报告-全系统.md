# 沃朗科技租赁板块 · 整合验收报告（全系统）

> 日期：2026-08-10　|　分支：main　|　状态：**冲刺0 + M1–M5 全部业务开发完成，本地整合验收通过，deploy-ready**
> 两道红线（真 prod 部署 / 真 SSO）按约定未碰——见《deploy/部署runbook.md》上线红线清单。

## 一、规模
| 维度 | 数量 |
|---|---|
| Git commit（main） | 58+ |
| Flyway 迁移 | V1–V14（43 张 `yc_rent_` 表） |
| 后端模块 | 25（quote/supplier/customer/asset/contract/billing/purchase/transfer/maintenance/stocktake/distribution/cashflow(finance)/monthly/bi/pdca/task/approval/roster/imports/file/workbench/analytics/reminder/rule/ai…） |
| 前端页面 | 19（报价/供应商/客户CRM/设备/合同/收租/凭证/现金流/月报/转让/维保/盘点/任务/花名册/BI-PDCA/导入/工作台/采购/驾驶舱） |

## 二、整合 Smoke（本地全新 boot·23 模块接口）
后端全新 boot：Flyway V1→V14 全部 apply，`Started RentApplication`，health 200。
**23/23 模块接口 HTTP 200**：health · 供应商 · 客户管道 · 设备台账 · 合同 · 采购 · 收租单 · 凭证 · 现金流驾驶舱 · 兑付缺口 · 回报四源 · 月度报表 · 转让 · 维保 · 盘点 · 任务 · 花名册 · 工作台聚合 · BI(在租率/账龄) · PDCA(看板/项) · 导入中心 · 500万红线 · cron登记。

## 三、各里程碑验收摘要（每波真测·证据见 PHASE5-VERIFICATION-LOG.md）
| 里程碑 | 关键验收证据 |
|---|---|
| 冲刺0 报价器 | 浏览器实测 6718(IRR)/6720(速算)/税后IRR25%/三层回报25·50·65.8%/占用40563，全 rule_config 出、对平设立方案书 |
| M1 供应商 | 池+履约雷达+单一依赖灯(货架0家亮灯)+淘汰留痕 |
| M1 客户CRM | 池+销售管道(加权101万)+信用画像/LTV/敞口+跟进+风控准入；**服务端隔离:越权403·LP不见成本** |
| M1 设备 | 逐件+序列号+7态状态机+配件树BOM+成本/残值即时算+故障档案 |
| M1 合同 | 签约自动生成N期租金计划+勾稽508000 balanced+单笔P&L 73840+作废/续租/提前结清逆向 |
| M1 采购 | 无合同拒采购+入库逐件生成设备+应付三段+退货红冲 |
| M1 RBAC | @RequireRole统一切面+越权403+audit留痕 |
| M2 收租 | 到期生成cron+核销回写+红冲(**DB硬幂等ERROR1062**+锁账400)+逾期三步走自动开案+还款恢复+收回→设备待处置 |
| M3 凭证双账 | 核销生成税务+经营双账借贷平衡+稽核缺凭证5→0；折旧表逐月递减(book_value唯一写手)；凭证红冲P0-F |
| M3 分配 | 净利50万→管理费10%档→可分配45万→现金22.5/滚存22.5→按比例分四出资方；period幂等+冲销 |
| M3 现金流/兑付缺口 | 应收应付分层+净现金流曲线；**P0-H:造80万应付到期→-21万→🔴红灯+裁决人+补款来源** |
| M3 回报四源 | 12+7.5+6+4.5=30% reconciled |
| M3 月度报表 | 六件套聚合不重算+经营分析七节(AI只起草·数字引package_json)+财务日历+POI导出Excel7894字节/Word3989字节+分配表按角色 |
| M4 转让 | transfer拆头+行逐台gain+残值双账凭证+资产出账+**名义价守卫无理由400强制索理由→审批** |
| M4 复投/维保/盘点 | 收回→再投放/二手/报废；维保质保内转供应商+fault_count回写；盘点差异走调整单不直改台账 |
| M5 任务/审批/花名册 | 任务绑角色转派留痕+完成校验；投放审批300万自主/超额协商；提成降本×5%/成交×1%从管理费列支 |
| M5 工作台/BI/PDCA | 工作台按角色不同(老板全量vsLP仅回报)；BI多维矩阵；PDCA红绿灯+到期回查+AI综述 |
| M5 审计/导入/对象存储 | audit UPDATE/DELETE触发器拒(ERROR1644)；导入走同一service事件流+公式转义+越权拒；签名URL过期/篡改/越权403 |

## 四、deploy-ready 套件（deploy/）
- `backend.Dockerfile`（多阶段 maven17→jre8·target=8）、`frontend.Dockerfile`（node22→nginx）、`frontend-nginx.conf`（/api反代+**已内置 P0-C 清空 X-User-\* 头**）、`docker-compose.prod.yml`（自带mysql+backend+frontend·健康检查·`.env`注入）、`.env.example`、`部署runbook.md`（含上线红线清单）。
- 后端生产镜像本地 `docker build` 验证：见 PHASE5 日志/交付说明。

## 五、上线红线清单（需你/门户团队·AI不能代做）
1. prod 服务器/域名目标（你提供）　2. 真SSO凭据（门户团队发 app_id/client_secret，走 ole-portal-sso 换适配层）　3. P0-C 网关剥离 X-User-*（已写进 nginx，上游中央网关也须做）　4. rsync 部署 exclude .env　5. 密钥走 env/KMS　6. 审计法律级抗抵赖（哈希链/WORM 占位待办）　7. 真 LLM key 可选（现全 mock）。
**部署授权**：真 prod 上线属对外不可逆，需你说"部署"并给目标后执行。

## 六、诚实说明 · 占位/待强化
- LLM 全 mock 不外呼（无 key）；三层杠杆③融资=0（融资模块未启用）；回报四源权重为 rule_config 业务口径（非逐单蒙特卡洛）；LP 份额投影待真 user 表（investor.user_id 现空）；导入测试在 dev 库留少量样本（标注即可）；折旧为直线法；审计法律级抗抵赖为占位。
