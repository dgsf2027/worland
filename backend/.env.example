# 后端环境变量示例(复制为 .env 或注入进程环境)

# --- 数据库(默认值已写在 application.yml,本地一般不用改) ---
# DB_URL=jdbc:mysql://127.0.0.1:3308/worland_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
# DB_USERNAME=root
# DB_PASSWORD=vend123

# --- SSO(ole 澳乐门户,暂用占位头适配层,不启用) ---
SSO_ENABLED=false
