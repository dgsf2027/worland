# 沃朗科技租赁板块 · 全自动开发交付报告（地基 + 冲刺0 + M1）

> 日期：2026-08-08　|　状态：**本地可跑、真测通过**　|　分支：main（20 commit）
> 目标（你外出前拍板）：地基 + 冲刺0 + M1 业务地基，本地可跑可真测。两道红线（prod 部署 / 真 SSO）按约定未碰。

---

## 一、怎么跑（回来直接点开验看）
> 后端与前端我已启动，可能仍在运行；若已停，按下面重启。

**后端**（Spring Boot @8082）
```bash
cd ~/系统开发/租凭公司/backend
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
mvn -s settings.xml spring-boot:run
```
**前端**（Vite @5175）
```bash
cd ~/系统开发/租凭公司/frontend
node node_modules/vite/bin/vite.js --host
```
浏览器打开 **http://localhost:5175/** → 左侧菜单：报价测算器 / 供应商·上游 / 客户·CRM / 设备·逐件台账 / 合同·租金计划 / 工作台。
（DB = docker `vend-mysql`:3308 库 `worland_dev`；已有种子数据，可直接点。）

---

## 二、建成了什么（真测通过）
| 模块 | 内容 | 证据 |
|---|---|---|
| **地基** | Spring Boot+Vue3+MySQL+Flyway+Druid+MyBatis-Plus+Knife4j 脚手架；占位头 `UserContext` 适配层（SSO 就绪一处替换） | boot 成功·health 200 |
| **冲刺0 报价测算器** | 规则引擎读 rule_config，输出月租/三层回报/价值定价校验/税后IRR | **浏览器实测对平**：6718(IRR)/6720(速算)/25·50·65.8%/占用40563 |
| **供应商·上游** | 供应商池+四维/供货矩阵/价格构成/履约雷达+淘汰留痕+**单一依赖灯** | 浏览器：4家·恒丰86·货架0家亮灯 |
| **客户·CRM** | 客户池(100+筛选)+销售管道看板(加权预测)+信用画像/LTV/风险敞口+跟进时间线+风控准入 | curl+SQL+浏览器·加权101万 |
| **逐件设备+BOM** | 一台一条(序列号+7态状态机)+配件树(递归)+成本拆解+残值即时算+故障档案+单台收益 | 8台·状态机·事件流 |
| **合同+租金计划** | 签约自动生成 N 期计划+勾稽校验+每期租金构成+单笔P&L+作废/变更/续租/提前结清逆向 | SQL：36期·勾稽508000 balanced·净利73840 |
| **采购应付+退货** | 无合同拒采购+入库逐件生成设备+应付三段+退货红冲 | SQL：应付222000·退货红冲释放设备 |
| **投放/空置** | 交付起租+空置亮灯 | idle-alert 接口 |
| **RBAC 统一切面** | `@RequireRole`+拦截器+敏感操作×角色矩阵+audit 留痕 | **越权 403**·audit_log 7行 |

**数据模型**：19 张 `yc_rent_` 表（Flyway V1–V7），逐件建档+合同挂N台+双账基座+规则版本化 rule_config。
**关键铁律落地**：数字全走规则引擎（禁硬编码）、单一真相源、逆向路径、服务端隔离（LP 不见成本·越权403）、schema 走 Flyway。

---

## 三、诚实说明 · 还没做（M2–M5，下阶段）
- **前端页**：采购入库·应付页、工作台聚合红点页 未建（后端接口已就绪）。
- **M2 收租闭环**：rent_bill 收租/核销、红冲退款、逾期三步走执行。
- **M3 财务分配**：精确折旧表、双账凭证、管理费/分配、现金流驾驶舱、**账期兑付缺口预警**、月度报表。
- **M4 转让资产**：transfer_order 逐台处置、维保、扫码盘点。
- **M5 事人BI**：任务/审批、花名册提成、BI-PDCA-AI、导入中心、对象存储。
- **P0 强化**：红冲幂等键唯一约束（P0-F 强化）、逐件部分退货。

## 四、上线红线待办（需你/门户团队，AI 不能代做）
1. **P0-C 网关剥离头**：上线前网关必须剥离客户端 `X-User-*` 头再重注入，否则可伪造 `X-User-Role:老板` 绕过 RBAC（代码层无法自证，是部署拓扑）。
2. **真 SSO**：门户团队在 `yc_portal_system` 注册子系统 + 下发 `app_id`/`client_secret` + 提供 base_url/测试账号，之后走 `ole-portal-sso` 一处替换适配层。
3. **prod 部署**：等你说"部署"。

## 五、两处待你拍板（不阻塞）
1. 设立方案书 §品类准入表「10万→16667」疑为「30万」笔误，建议回传更正。
2. 精确月租已「IRR严格解 6718 + 速算起价 6720」并列（符合评审 P1-11），如需只取其一告诉我。
