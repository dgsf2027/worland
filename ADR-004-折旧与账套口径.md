# ADR-004：折旧计划表 + 账套口径下的 book_value

- 状态：已接受（2026-08-08，Phase 1.5 评审 P0-B）
- 关联：DESIGN_DOC v0.2 §4.1/4.3；补 ADR-003（仅覆盖 Finance Book，未覆盖折旧与 book_value 口径）

## 背景（评审 P0-B）
1. `asset.book_value`（账面净值）声明为"派生"，但只挂 `@writes=asset_event`——折旧是**按时间逐月**发生的，事件流覆盖不了，等于该真相源字段**无写手**（§4.17 stale 红线）。
2. DESIGN v0.1 把 `rent_schedule`（客户应收租金计划）错标为 Odoo `account.asset.line`——后者是**资产折旧计划行**，与收款计划是两张不同的表（概念错位）。
3. `book_value/residual_value` 是**账套相关**的：税务口径"分期收款销售"下设备交付即结转成本、变应收（可能无在账资产）；经营口径才按自有租赁资产算折旧/残值。单标量 `book_value` 装不下两套账。

## 决策
1. **新增 `asset_depreciation_line`（折旧计划真相源）**：`asset_id, book(ops), period_no, depr_amount, book_value_after, voucher_id`。折旧按月生成（cron/规则引擎），`asset.book_value` 由本表即时算或回填——`book_value` 有唯一写手。
2. **`book_value/residual_value` 仅经营口径（ops 账）**；税务口径走应收，不在 asset 上体现两套值。
3. **`residual_value` 不落列**，由 `_helpers` 按部件残值×折旧曲线即时算（防 stale）。
4. **术语更正**：`rent_schedule`＝应收/收款计划（收款态 owner=rent_bill）；`account.asset.line` 的对标物＝新增的 `asset_depreciation_line`。二者不再混用。

## 后果
- ✅ book_value 有写手、可对平；折旧/残值/单台 P&L 口径清晰；双账不打架。
- ⚠️ 需实现月度折旧生成任务 + 折旧凭证（经营口径）；冲刺0/M3 对平设立方案书残值(10%/30%)。
- 关联 P1：单台 P&L 依赖 `contract_asset.alloc_rent` + 本折旧表。

## 备选（放弃）
- book_value 纯事件驱动：折旧覆盖不了，stale。
- 单账 book_value：装不下税务/经营两口径。
