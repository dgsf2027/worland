# 曜石科技租赁板块 · 上线前置清单（公网部署要办的事）

> 日期：2026-08-14　|　当前：本地生产栈已跑通（localhost:8088），距公网上线差本清单几件人工/门户事项。
> 原则：凭据/密钥（SSO client_secret、DB 密码、LLM key）**你自己填进 `deploy/.env`（不进 git）**，AI 不代持有、不代输入。

---

## 一、你（或运维）要准备的
| # | 事项 | 说明 | 谁办 |
|---|---|---|---|
| 1 | **prod 服务器** | 一台能公网访问的 Linux 服务器，装好 Docker + docker compose；给我 SSH 访问方式 | 你/运维 |
| 2 | **域名 + HTTPS** | 域名解析到服务器；HTTPS 证书（或走你们中央网关/Caddy 统一签发） | 你/运维 |
| 3 | **prod DB 密码** | 一个强密码，填进服务器上的 `deploy/.env` 的 `DB_PASSWORD` | 你 |
| 4 | **门户 SSO 凭据** | 让门户团队在 `yc_portal_system` 注册子系统"曜石租赁"，拿到 **`app_id` / `client_secret` / `SSO_PORTAL_BASE_URL`** + 一个能登录门户的测试员工号 | **门户团队** |
| 5 | （可选）真 LLM | 若要 AI 月报/解读真生效：`LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL`（OpenAI 兼容），填进 `.env` | 你 |

> 第 4 项是硬阻塞——没有门户凭据，就只能占位头登录、不能给真实员工用。

## 二、你给齐后，我来做的
1. **换真 SSO**：把占位头 `UserContext` 适配层换成门户 JWT（走 skill `ole-portal-sso`：`/v1/sso/exchange` 换 token、`/v1/sso/public-key` 验签、共库消费 `yc_portal_member`）。
2. **nginx 翻红线**：**同时**开①剥离客户端自带 `X-User-*` ②鉴权层按 SSO 解析结果重注入 `X-User-*`（今天踩过：只剥不注入=整站 401）。
3. **部署到服务器**：传代码/镜像 → `docker compose up -d`（Flyway 自动建全 43 表）→ 健康检查 + 穿 nginx + SSO 真登录 V0–V5 真测验真。
4. **等你说"部署"** 才推 prod（对外不可逆红线）。

## 三、一键部署命令（在 prod 服务器上）
```bash
# 1. 传代码(含 deploy/,但 .env 单独安全传/服务器上现填,不从 git 带)
rsync -avz --exclude='.env' --exclude='node_modules' --exclude='target' 项目/ user@prod:/data/apps/worland/
# 2. 服务器上填 .env(强密码/凭据),再起栈
cd /data/apps/worland && cp deploy/.env.example deploy/.env && vi deploy/.env   # 填 DB_PASSWORD / SSO_* / LLM_*
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env up -d --build
# 3. 验
curl -s http://localhost:8088/api/v1/health         # 200
docker compose -f deploy/docker-compose.prod.yml ps # 三容器 healthy
```

## 四、红线提醒（部署纪律）
- **`.env` 永不进 git**（已在 .gitignore）；rsync 必带 `--exclude='.env'`（覆盖 prod .env = 全站 outage）。
- **DB 密码 / SSO client_secret / LLM key** 只在服务器 `.env` 里，不写进代码/compose/仓库。
- prod schema 由 Flyway 随 boot 自动 apply；**改 schema 的更新**要确认迁移已 apply 再放流量。
- SSO 上线 = nginx 剥离 + 重注入**同时**开，缺一即 401 或可伪造。

## 五、当前可先用
- **公网试用（已上线）**：**https://zulin.aoleplat.com** — 2026-08-15 接入中央网关，Let's Encrypt 生产证书已签发（有效期至 11/13），全链路验证通过（首屏/API 健康/工作台 KPI 真数据）。
- 本地生产栈：**http://localhost:8088**（占位头登录，右上角切角色）。

> ⚠️ **公网试用期安全须知（占位头模式）**：当前 nginx 处于占位期（放行客户端 `X-User-*`，未接 SSO），意味着**任何访问者都能在右上角自选"老板"角色看全量数据**——这是 ADR-001 约定的占位期取舍。因此公网站**只放演示/脱敏数据，别录真实经营数据**；等门户 SSO 凭据到位、翻开 nginx 剥离+重注入红线（第二节第 2 步）后，才是可给真实员工用的鉴权态。
